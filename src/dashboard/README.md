Some of the dashboards require the database table `api_perf_log`.
Note that this table is not included in the standard schema and it has to be populated using the dedicated script `ingest_api_logs.py`.
